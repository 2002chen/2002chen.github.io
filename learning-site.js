subsite.ready.then(async context=>{if(!context)return;const {client,session}=context;document.getElementById('learningEmail').textContent=session.user.email||'';const [progress,attempts]=await Promise.all([client.from('section_progress').select('*',{count:'exact',head:true}).eq('completed',true),client.from('quiz_attempts').select('is_correct')]);document.getElementById('sectionProgress').textContent=progress.count??0;const rows=attempts.data||[];document.getElementById('attemptCount').textContent=rows.length;document.getElementById('accuracy').textContent=rows.length?`${Math.round(rows.filter(x=>x.is_correct).length/rows.length*100)}%`:'0%'});
